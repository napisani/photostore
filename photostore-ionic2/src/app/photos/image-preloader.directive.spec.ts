import { ImagePreloaderDirective } from './image-preloader.directive';

describe('ImagePreloaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagePreloaderDirective();
    expect(directive).toBeTruthy();
  });
});
